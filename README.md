# Notas:

```
Options:
      --help     Show help                                   [boolean]
      --version  Show version number                         [boolean]
  -b, --base     Base de la tabla de multiplicar   [number] [required]
  -l, --listar   Muestra la tabla de multiplicar en consola  [boolean]
  -h, --hasta    Hasta que numero ira la tabla de multiplicar [number] [default: 10]
```